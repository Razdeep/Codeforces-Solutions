// https://codeforces.com/contest/1433/problem/D
// NOT SOLVED
#include <iostream>
#include <vector>
using namespace std;

int main() {
    int tc;
    cin >> tc;

    while (tc--) {
        int n;
        cin >> n;

        vector<int> gang(n);
        vector<bool> visited(n + 1, false);

        for (int i = 0; i < n; ++i) {
            cin >> gang[i];
        }

        
    }
    return 0;
}