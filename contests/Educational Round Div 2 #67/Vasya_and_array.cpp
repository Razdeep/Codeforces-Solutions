// https://codeforces.com/contest/1187/problem/C
// NOT SOLVED
#include <bits/stdc++.h>
#define For(i, n) for (__typeof(i) i = 0; i < n; i++)
#define all(v) v.begin(), v.end()
typedef long long ll;
using namespace std;
int main()
{
	ll n, m, i;
	cin >> n >> m;
	vector<ll> t(m, 0LL), l(m, 0LL), r(m, 0LL);
	For (i, m)
	{
		cin >> t[i] >> l[i] >> m[i];
		
	}
	return 0;
}
