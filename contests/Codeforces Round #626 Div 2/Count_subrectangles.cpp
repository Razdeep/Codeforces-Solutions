// https://codeforces.com/contest/1323/problem/B
// @TODO
#include <iostream>
#include <vector>
using namespace std;

int main()
{
	int rows, columns, area;
	cin >> rows >> columns >> area;
	
	vector<int> a(rows), b(columns);

	for (int &it_a: a)
		cin >> it;
	
	for (int &it_b: b)
		cin >> it_b;

	
	

	return 0;
}
