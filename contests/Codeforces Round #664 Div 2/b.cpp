// 

#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>
#define all(v) v.begin(), v.end()
#define trace(x) cout << #x <<  ": " << x << endl;
using namespace std;
typedef long long ll;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	int tc = 1;
	cin >> tc;

	while (tc--) {
		int n;
		cin >> n;

		vector<int> arr;

		for (int i = 0; i < n; ++i) {
			cin >> arr[i];
		}
	}
	return 0;
}

