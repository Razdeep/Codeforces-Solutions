// https://codeforces.com/problemset/problem/1105/B
// NOT SOLVED
#include <bits/stdc++.h>
#define all(v) v.begin(), v.end()
using namespace std;
typedef long long ll;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    string text;
    ll length, minimum_occurence;
    cin >> length >> minimum_occurence >> text;
    std::pair<char, ll> maintainer;

    return 0;
}